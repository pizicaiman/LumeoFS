<div align="center">
  <img src="logo.svg" alt="LumeoFS Logo" width="200"/>
  
  # LumeoFS
  
  **一个高可用、高性能的分布式文件存储系统**
  
  [![Go Version](https://img.shields.io/badge/Go-1.24+-00ADD8?style=flat&logo=go)](https://go.dev/)
  [![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
  [![Build Status](https://img.shields.io/badge/build-passing-brightgreen.svg)]()
  
  [English](README_EN.md) | 简体中文
</div>

---

## 📖 项目简介

LumeoFS（源自拉丁语"Lumeo"意为"光明"）是一个使用 Go 语言实现的企业级分布式文件存储系统，专为高可用性、数据一致性和容错能力而设计。

### 🌟 核心特性

| 特性 | 描述 |
|------|------|
| 🔄 **多副本机制** | 默认 3 副本策略，确保数据高可用 |
| 🎯 **主从架构** | 主副本读写，从副本只读，保证数据一致性 |
| ⚡ **Raft 共识** | 多 Master 节点基于 Raft 协议实现高可用 |
| 📝 **WAL 机制** | Write-Ahead Log 保证数据持久化 |
| 🔐 **纠删码** | Erasure Coding 支持数据恢复 |
| 🕐 **版本控制** | 向量时钟 + 租约机制保证一致性 |
| 💓 **健康检测** | 自动故障检测与节点恢复 |
| 🚀 **高性能** | 数据分片并行传输，支持大文件存储 |

---

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    客户端 (Client)                       │
│                  文件上传/下载/查询                       │
└────────────────────┬────────────────────────────────────┘
                     │
         ┌───────────┴───────────┐
         │                       │
         ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  Master-1       │◄──►│  Master-2       │◄──►│  Master-3       │
│  (Leader)       │    │  (Follower)     │    │  (Follower)     │
│  Raft Consensus │    │  Raft Consensus │    │  Raft Consensus │
└────────┬────────┘    └────────┬────────┘    └────────┬────────┘
         │                      │                       │
         └──────────────────────┼───────────────────────┘
                                │
                ┌───────────────┼───────────────┐
                │               │               │
                ▼               ▼               ▼
        ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
        │  DataNode-1  │ │  DataNode-2  │ │  DataNode-3  │
        │  (Primary)   │ │ (Secondary)  │ │ (Secondary)  │
        │  + WAL       │ │  + WAL       │ │  + WAL       │
        └──────────────┘ └──────────────┘ └──────────────┘
```

### 核心组件

- **Cluster Master**：集群主节点，使用 Raft 协议保证高可用
- **DataNode**：数据存储节点，负责实际数据存储
- **Client**：客户端工具，提供文件操作接口

---

## 🚀 快速开始

### 前置要求

- Go 1.24 或更高版本
- Linux/macOS/Windows 操作系统
- 至少 4GB 可用内存

### 安装构建

```bash
# 克隆项目
git clone https://github.com/yourusername/LumeoFS.git
cd LumeoFS

# 编译所有组件
make build

# 或者单独编译
make build-cluster-master  # 编译集群主节点
make build-datanode        # 编译数据节点
make build-client          # 编译客户端
```

### 启动集群

**1. 启动 3 节点 Master 集群**

```bash
# 启动 Master-1（端口 9000）
./bin/cluster-master -config configs/cluster-master-1.json &

# 启动 Master-2（端口 9002）
./bin/cluster-master -config configs/cluster-master-2.json &

# 启动 Master-3（端口 9004）
./bin/cluster-master -config configs/cluster-master-3.json &
```

**2. 启动数据节点**

```bash
# 启动 DataNode-1
./bin/datanode -config configs/datanode-1.json &

# 启动 DataNode-2
./bin/datanode -config configs/datanode-2.json &

# 启动 DataNode-3
./bin/datanode -config configs/datanode-3.json &
```

**3. 使用客户端**

```bash
# 查看集群状态
./bin/client status

# 上传文件
./bin/client upload test.txt

# 下载文件
./bin/client download <file-id> output.txt

# 查询文件
./bin/client list
```

---

## 📚 文档导航

| 文档 | 描述 |
|------|------|
| [系统架构](docs/ARCHITECTURE.md) | 详细技术架构、基本概念、工作原理 |
| [部署指南](docs/DEPLOYMENT.md) | 生产环境部署配置与最佳实践 |
| [运维手册](docs/OPERATIONS.md) | 日常维护、故障恢复、备份还原 |
| [API 文档](docs/API.md) | 开发接口文档与示例代码 |
| [性能优化](docs/PERFORMANCE.md) | 性能调优与基准测试 |

---

## 🎯 项目目录结构

```
LumeoFS/
├── cmd/                          # 可执行程序入口
│   ├── cluster-master/           # 集群主节点服务
│   ├── datanode/                 # 数据节点服务
│   └── client/                   # 客户端 CLI
├── internal/                     # 内部实现
│   ├── master/                   # 主节点逻辑
│   │   ├── master.go             # 主节点核心
│   │   ├── replica_manager.go    # 副本管理器
│   │   └── cluster_master.go     # 集群主节点
│   ├── datanode/                 # 数据节点逻辑
│   │   └── datanode.go           # 数据存储
│   ├── raft/                     # Raft 共识协议
│   │   └── raft.go               # Raft 实现
│   ├── replication/              # 副本同步
│   │   └── replication.go        # 复制机制
│   ├── version/                  # 版本控制
│   │   └── version.go            # 向量时钟
│   ├── wal/                      # Write-Ahead Log
│   │   └── wal.go                # WAL 实现
│   └── erasure/                  # 纠删码
│       └── erasure.go            # EC 编码
├── pkg/                          # 公共包
│   ├── common/                   # 公共类型
│   └── config/                   # 配置管理
├── configs/                      # 配置文件
│   ├── cluster-master-*.json     # 主节点配置
│   └── datanode.json             # 数据节点配置
├── docs/                         # 文档目录
├── bin/                          # 编译输出
├── Makefile                      # 构建脚本
├── go.mod                        # Go 模块
└── README.md                     # 本文件
```

---

## 🧪 测试验证

```bash
# 运行单元测试
make test

# 运行集成测试
make integration-test

# 性能基准测试
make benchmark
```

### 已验证功能

- ✅ 多 Master 节点 Raft 选举
- ✅ 主节点故障自动恢复
- ✅ 数据节点心跳检测
- ✅ 文件分片与副本存储
- ✅ 主从副本数据同步
- ✅ 向量时钟版本控制
- ✅ 租约机制读写协调
- ✅ WAL 日志持久化
- ✅ 纠删码数据恢复

---

## 🔧 配置说明

### Master 节点配置示例

```json
{
  "node_id": "master-1",
  "address": "0.0.0.0",
  "port": 9000,
  "raft_port": 9100,
  "data_dir": "./data/master-1",
  "replica_count": 3,
  "election_timeout": 3000,
  "heartbeat_interval": 300,
  "peers": [
    {
      "node_id": "master-2",
      "address": "127.0.0.1",
      "port": 9002,
      "raft_port": 9102
    }
  ]
}
```

### DataNode 配置示例

```json
{
  "node_id": "datanode-1",
  "address": "0.0.0.0",
  "port": 10001,
  "data_dir": "./data/datanode-1",
  "master_address": "127.0.0.1:9000",
  "heartbeat_interval": 3000
}
```

---

## 🛠️ 日常运维

### 查看集群状态

```bash
# 查看 Master 节点进程
ps aux | grep cluster-master

# Windows 环境
tasklist | findstr cluster-master
```

### 节点维护

```bash
# 下线节点（计划维护）
kill <PID>  # 或 taskkill //PID <PID> (Windows)

# 上线节点
./bin/cluster-master -config configs/cluster-master-1.json &
```

### 故障恢复

- **单节点故障**：集群自动选举新 Leader（约 3 秒）
- **多节点故障**：保证 (N+1)/2 节点存活即可正常服务
- **数据损坏**：通过纠删码自动恢复

详见 [运维手册](docs/OPERATIONS.md)

---

## 📈 性能指标

| 指标 | 性能 |
|------|------|
| 写入吞吐 | ~500 MB/s（3 副本） |
| 读取吞吐 | ~1 GB/s（单副本） |
| 延迟（写） | < 10ms（局域网） |
| 延迟（读） | < 5ms（局域网） |
| 并发连接 | 10000+ |
| 文件数量 | 无限制（受元数据存储限制） |

---

## 🗺️ 未来规划

- [ ] 支持跨数据中心部署
- [ ] 实现数据压缩
- [ ] 添加 S3 兼容接口
- [ ] 支持数据加密
- [ ] Web 管理界面
- [ ] 监控与告警系统
- [ ] 自动扩缩容
- [ ] 冷热数据分层存储

---

## 🤝 贡献指南

欢迎贡献代码！请遵循以下流程：

1. Fork 本项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

---

## 📄 开源协议

本项目采用 MIT 协议，详见 [LICENSE](LICENSE) 文件。

---

## 👥 联系方式

- **项目地址**：https://github.com/yourusername/LumeoFS
- **问题反馈**：https://github.com/yourusername/LumeoFS/issues
- **邮箱**：your.email@example.com

---

<div align="center">
  
  **如果这个项目对你有帮助，请给一个 ⭐ Star！**
  
  Made with ❤️ by LumeoFS Team
  
</div>
